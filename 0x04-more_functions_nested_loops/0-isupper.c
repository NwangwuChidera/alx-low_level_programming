#include "main.h"

/**
 * _isupper - Entry point
 * main - prints a function that checks uppercase
 * @c: the ASCII value
 * Return: 1 if true. 0 if false
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
