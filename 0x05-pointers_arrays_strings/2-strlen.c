#include "main.h"
/**
 * _strlen - This function returns the length of a string
 *@s: This is pointer
 *Return: 0 Always
 */
int _strlen(char *s)
{
	char *s;
	int len;

	len = _strlen(str);
	echo strlen("*s");
}
